{
  "mcpServers": {
    "sessions": {
      "type": "stdio",
      "command": "python",
      "args": [".sessions/tools/sessions_mcp_server.py"],
      "description": "Semantic search over project sessions using RAG"
    }
  }
}
