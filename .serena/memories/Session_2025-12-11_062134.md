# Session 2025-12-11: Advanced Dataset Selection - DETR Q81 + Reports

## Wykonane zmiany

### 1. DETR EL2N Scorer - Query 81 Only
- `DETR_QUERY_ID = 81` jako stała
- `detection_info` dict: q81_score, q81_box, has_detection, el2n_score
- `save_selected_visualizations()` - rysuje Q81 bbox
- `unload()` dla GPU memory

### 2. Sekwencyjne ładowanie modeli
```
Fourier (CPU) → DINO (GPU) → unload → SAM3 (GPU) → unload → DETR (GPU) → unload
```

### 3. Raporty selekcji
- `generate_selection_report()` + `save_selection_report()`
- Output: `selection_reasons.json` + `selection_reasons.txt`

### 4. Main pipeline integration
- Wywołanie `save_selection_report()` po selekcji
- Wywołanie `save_selected_visualizations()` dla DETR Q81

## Pliki zmodyfikowane
- `AdvancedDatasetSelection/selection_methods/detr_el2n_scorer.py`
- `AdvancedDatasetSelection/selection_methods/cluster_selector.py`
- `AdvancedDatasetSelection/feature_extractors/sam_extractor.py`
- `AdvancedDatasetSelection/feature_extractors/dino_extractor.py`
- `AdvancedDatasetSelection/main_selection_pipeline.py`

## KRYTYCZNY TODO

**Problem:** EL2N spłaszcza wszystkie próbki poniżej progu 0.3 do tej samej trudności.
- Q81=0.05 i Q81=0.25 mają TEN SAM difficulty=1.0

**Rozwiązanie:** Dodać filtr `q81_max_score: 0.2` w config dla hard sample selection.

## Pełna dokumentacja
`.sessions/Session_2025-12-11_062134/SESSION_SUMMARY.md`
