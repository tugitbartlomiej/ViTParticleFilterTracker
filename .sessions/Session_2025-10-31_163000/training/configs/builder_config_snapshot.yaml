# Snapshot of dataset builder config used

source_videos_dir: "E:/Cataract/videos/micro"
output_dir: "F:/Studia/PhD_projekt/VIT/ViTParticleFilterTracker/output_cataract"

sampling:
  target_fps: 2
  max_frames_per_video: 1500
  blur_threshold: 80
  phash_threshold: 10

category:
  id: 1
  name: "tooltip"

yolo_pseudo_labels:
  enabled: true
  weights_path: "F:/Studia/PhD_projekt/VIT/ViTParticleFilterTracker/YOLO_DETR_Benchmarks/models/YOLO/epoch100.pt"
  conf_threshold: 0.25

negatives:
  include: true
  max_ratio: 0.3

splits:
  train: 0.7
  val: 0.15
  test: 0.15
